PORT=4000

# postgresql | json (auto-falls back to json if postgres unavailable)
# DB_MODE=json

DATABASE_URL=postgresql://sgpro:sgpro@localhost:5432/sgpro
JSON_DB_PATH=data/sgpro.json

WP_BASE_URL=https://sharvaconsulting.com
UPLOADS_DIR=uploads

JWT_SECRET=sgpro-demo-secret-key-change-me-0xA1B2C3D4E5F6
JWT_ISSUER=sgprogrowth-admin
JWT_AUDIENCE=sgprogrowth-portal
CORS_ORIGIN=http://localhost:5173

# Required by `npm run db:seed` — password used to seed admin/demo users.
# Set this in your local .env (not committed) to your actual admin password.
ADMIN_SEED_PASSWORD=change-me-before-seeding

# AI Assistant (OpenAI-compatible — also works with Groq, Together, Azure OpenAI, etc.)
# LLM_API_KEY=sk-...
# LLM_BASE_URL=https://api.openai.com/v1
# LLM_MODEL=gpt-4o-mini
